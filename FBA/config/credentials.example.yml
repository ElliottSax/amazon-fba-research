# Amazon SP-API Credentials
# Copy this file to credentials.yml and fill in your values
# Location: ~/.sp-api/credentials.yml (default) or specify path in code

# Required - Get from Seller Central > Apps & Services > Develop Apps
refresh_token: 'Atzr|your-refresh-token-here'
lwa_app_id: 'amzn1.application-oa2-client.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx'
lwa_client_secret: 'your-lwa-client-secret'

# Optional - For role-based access (if using AWS IAM)
# role_arn: 'arn:aws:iam::123456789012:role/your-sp-api-role'

# Optional - Named profiles for multiple accounts
# version: '1.0'
# default:
#   refresh_token: 'Atzr|default-token'
#   lwa_app_id: 'amzn1.application-oa2-client.default'
#   lwa_client_secret: 'default-secret'
#
# eu_account:
#   refresh_token: 'Atzr|eu-token'
#   lwa_app_id: 'amzn1.application-oa2-client.eu'
#   lwa_client_secret: 'eu-secret'
